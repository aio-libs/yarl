Skipped the ``str.replace()`` scan for ``@:#?`` in
``_check_netloc()`` when the character is absent, mirroring the
short-circuit already used for unsafe bytes in ``split_url()``
and avoiding allocations on the URL parsing hot path
-- by :user:`aiolibsbot`.
