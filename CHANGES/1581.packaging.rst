Restored coverage collection for Cython modules in CI, which had been
reporting nothing since line tracing became opt-in. CI now also fails if
the ``*.pyx`` files go unmeasured again, rather than silently dropping
them from the report -- by :user:`rodrigobnogueira`.
